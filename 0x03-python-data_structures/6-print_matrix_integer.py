def print_matrix_integer(matrix=[[]]):
    for row in matrix:
        for col in row:
            if (col != 0):
                print(col, end=" ")
            elif (col == ):
                print("")
            else:
                print(col, end="")
        print()
